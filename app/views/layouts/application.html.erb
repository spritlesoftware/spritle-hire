<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Spritle - Online Test</title>
    <%= favicon_link_tag asset_path('favicon.ico'), :rel => 'icon', :type =>  'image/png' %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render "/layouts/toast_flash_messages" %>

    <div class="container-scroller">
      <% if current_user == nil %>
        <%= yield %>
      <% elsif current_user != nil && current_user.is_admin? %>
        <%= render "/layouts/top_nav_bar" %>

        <div class="container-fluid page-body-wrapper">
          <%= render "/layouts/side_nav_bar" %>

          <div class="main-panel">
            <%= yield %>

            <%= render '/layouts/footer'%>
          </div>
        </div>
      <% elsif current_user != nil && current_user.is_candidate? %>
        <%= render "/layouts/top_nav_bar" %>

        <div class="container-fluid page-body-wrapper">
          <%= yield %>
        </div>
      <% end %>
    </div>
  </body>
</html>
